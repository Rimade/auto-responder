# HH.ru Auto Responder v2.0+ 🚀

Мощный скрипт для автоматизации массовых откликов на HH.ru: расширенный интерфейс, гибкие настройки, актуальные лимиты, интеллектуальное логирование и поддержка любых сценариев отклика.

---

## 🌟 Новейшие возможности (v2.0+)

- **🎯 Любые URL HH.ru:** поиск, карточки, главная, директ-ссылки на вакансии, любые разделы — работает везде.
- **🎨 Современный UI:** адаптивная панель в правом нижнем углу (и для мобилок, и для десктопа, как с touch, так и с мышью), полностью независимая от DOM HH.
- **📊 Максимум статистики:** реальное отображение: успешно отправленных, пропущенных (с пояснением причины), ошибок API/сети, лимитов, стопов, автопауз.
- **🔍 Двойная диагностика:** автоматическая самопроверка аккаунта, токенов, ключевых ошибок, подхват событий из интерфейса и браузера.
- **🔔 Интерактивное информирование:** цветные pop-up уведомления обо всём важном — cпец-иконки, временные задержки, авто-звук по важным событиям, расширенный log.
- **⏸️ Пауза/остановка:** можно приостанавливать и возвращаться, полный stop по ошибкам или вручную, однокнопочное управление.
- **💾 Экспорт логов:** в CSV/JSON – по каждой вакансии, по всем откликам, по ошибкам, для самостоятельного анализа и отчёта.
- **🧹 Мгновенное очищение истории:** одним нажатием сброс логов, статистики, состояния (серии откликов, фильтры).
- **🛡️ Авто-защитные функции:** автопауза при повторных ошибках, лимитах, новых условиях HH.ru, предотвращение переоткликов.
- **📝 Настраиваемый шаблон письма:** поддержка подстановок, в т.ч. {#vacancyName}, и минимальный контроль случайных дубликатов по тексту.
- **💡 Рекомендации в реальном времени:** при ошибках и событиях отображаются конкретные подсказки, сверх всплывающих уведомлений.

---

## 🚦 Быстрый старт

### 1. Найдите и вставьте свой `resume_hash`:

1. Авторизуйтесь на [hh.ru](https://hh.ru).
2. Перейдите в своё резюме.
3. Откройте вкладку "Network"→ найдите любой запрос с `/resume/`, скопируйте хеш (всё после `resume/` в адресе).
4. Или используйте быстрый способ: то, что после слова `resume/` в адресе браузера — это и есть `resume_hash`.

### 2. Настройте скрипт

- Откройте `hh-auto-responder.js` любым редактором.
- Измените настройки в начале файла:
    ```js
    const CONFIG = {
      RESUME_HASH: 'ваш_хеш_резюме_здесь', // Без него ничего не работает
      COVER_LETTER_TEMPLATE: 'Ваше личное сопроводительное. Можно {#vacancyName}!',
      MAX_RESPONSES_PER_DAY: 200,    // Какой хотите лимит (по умолчанию 200)
      DELAY_BETWEEN_RESPONSES: 3000, // задержка между откликами в миллисекундах
      DELAY_BETWEEN_PAGES: 5000,     // задержка между пакетами вакансий (страницами)
      MAX_RETRIES: 2,                // сколько раз пробовать при падениях API
      RETRY_DELAY: 2500              // задержка между попытками повтора
    };
    ```

### 3. Запустите в браузере

**Вариант A (единоразово/тест):**
1. Откройте нужную страницу HH.ru.
2. F12 → вкладка Console → вставьте содержимое `hh-auto-responder.js` → Enter.
3. Справа внизу отрисуется панель управления.

**Вариант B (через Scripty)**
1. Откройте https://scripty.abhisheksatre.com/
2. Вставьте код из `hh-auto-responder.js` в большое поле.
3. В "Site URL": используйте `https://hh.ru/*`
4. "Create Script" → добавьте в закладки браузера.
5. На любой странице HH.ru кликом запускаете рабочую панель (работает даже при перезагрузках).

---

## 👆 Использование и особенности интерфейса

### Кнопки панели

- **📤 Запустить** — массовая отправка, с динамически обновляющимся статусом.
- **⏸️ Пауза** — временная остановка, защитит от лимитов.
- **⏹️ Стоп** — немедленный полный сброс.
- **📊 Статистика** — детализация: успешно/пропущено/с ошибкой/с повтором.
- **💾 Экспорт** — выгрузка логов (csv/json).
- **🗑️ Очистить** — удаление истории откликов, фильтра, статистики.
- **⚙️ Настройки** — быстрый доступ к основным параметрам без смены страницы.

### Обработка любых ссылок
- `https://hh.ru/search/vacancy?...` — работает по списку.
- `https://hh.ru/vacancy/123456` — единичный отклик.
- `https://hh.ru/` — общий поиск (с учетом фильтров, если заданы).
- Все другие разделы hh.ru — автоматически распознаются.

---

## ⚒️ Настройки (CONFIG) и возможности

```js
const CONFIG = {
  RESUME_HASH: 'обязателен!',
  COVER_LETTER_TEMPLATE: 'Мое уникальное письмо для {#vacancyName}',
  MAX_RESPONSES_PER_DAY: 100,     // лимит за сутки (можно меньше!)
  DELAY_BETWEEN_RESPONSES: 2000,  // задержка между откликами (мс)
  DELAY_BETWEEN_PAGES: 4000,      // задержка между страницами поиска (мс)
  MAX_RETRIES: 3,                 // повторять при ошибках
  RETRY_DELAY: 2000               // попытки через столько (мс)
};
```
- ❗**RESUME_HASH обязателен** — без него ничего работать не будет.
- В письме можно использовать `{#vacancyName}`.
- Все параметры легко меняются – можно снизить задержки и лимиты для тестов.

---

## 🧠 Автодиагностика и защита от ошибок

Нажмите **🔍 Диагностика**:
- Проверка авторизации, валидности токенов и ключей.
- Проверка актуальности резюме.
- Проверка лимитов суток, текущих блоков, ошибки доступа к API.
- Подсветка некорректных настроек и рекомендаций прямо в UI.
- Любые ошибки немедленно заносятся в лог и выводятся уведомлением.

+ Крупные ошибки выводятся через отдельный pop-up и записываются для экспорта.
+ Есть глобальная ловушка ошибок: если что-то случается — подробность сохраняется в историю (см. обработчик global error).

---

## 🗃️ Логирование по-настоящему

- Все заявки, прохождения, ошибки (и системные, и API) — пишутся в `localStorage`:
    - `hh_api_log` — вся история запросов;
    - `hh_sent_responses` — id всех успешных.
    - `hh_stats` — ежедневная агрегированная статистика;
    - `hh_filter_url` — последний URL поиска для быстрого повтора.

- **Экспорт** — в два клика всё выгружается в CSV/JSON.
- **Очистить** — сразу освобождает место, сбрасывает логи и статистику, панель обновится автоматически.

- При ошибках или подозрении на бан — всё фиксируется для анализа.

---

## 💡 Лучшие советы работы

- Начинайте с лимита 10–20, HH может банить за резкий рост откликов!
- Используйте фильтры (зарплата, город, без отклика, и т.д.) — повышайте релевантность.
- Не переключайте страницу во время работы — постановка на автопаузу.
- Откалибруйте задержки (может понадобиться увеличить DELAY_BETWEEN_RESPONSES для надёжности).
- Экспортируйте логи для обратной связи и собственных записей.
- Используйте Scripty — панель появится всегда и не пропадёт после обновления страницы.

---

## 💠 Внутренняя структура и последние фишки

- **`UIBuilder`** — современный конструктор всех кнопок, модальных окон, интерфейса (адаптация под DOM HH, мобильные стилевые хаки).
- **`UI`** — все системные pop-up, динамическая подгрузка элементов, пользователи сами могут закрывать уведомления.
- **`Logger`** — всё логируется: id вакансий, ошибки (API и JS), экспорт без спецобработки, очищается в один клик.
- **`Responses`** — отслеживает отправку, дубли, вчерашние/старые ответы, предотвращает повторную рассылку.
- **`Utils`** — sleep, подсчёт времени, разбивка массивов на порции, трекинг лимитов и пауз.
- **`ProgressTracker`** — точная визуализация хода обработки: индикатор + цифры, моментальные обновления.
- **`STATE`** — всё-в-одном: флаги паузы, лимитов, ошибок, шаги цикла отклика.
- **Обработка ошибок (`window.onerror`, try/catch)** — любые неожиданные сбои пишутся отдельно (с текстом, callstack, временем и стат. сессии).

### Как реализовано:
- Глобальный обработчик ошибок (`window.onerror`) – фиксирует все сбои и записывает в лог с подробностями! Выводит всплывающее уведомление о "глобальной ошибке" с деталями:
    ```javascript
    window.addEventListener('error', (event) => {
      console.error('Глобальная ошибка:', event.error);
      if (STATE.settings.detailedLogging) {
        Logger.saveLog({
          id: 'system',
          title: 'Системная ошибка',
          time: new Date().toISOString(),
          success: false,
          message: event.error?.message || 'Неизвестная ошибка',
        });
      }
    });
    ```

- UI/панель полностью пересоздаётся при каждом запуске (чтобы не осталось мусора от прошлых сессий).
- При достижении лимита (или ошибке): автопауза, пользователь сразу получает визуальное уведомление.

### Пример инициализации и запуска:
```javascript
function startProcess(url) {
  // Проверяем хеш и лимиты
  // ...
  // Обновляем UI
  // ...
  // Запускаем основной процесс
  processAllPages(url);
}
```
(см. сам скрипт для полного рабочего цикла и детализированных статусов!)

---

## ⏫ История изменений

### v2.0 (последняя)

- Суперадаптивный UI и безопасный механизм запуска/остановки.
- Универсальное распознавание вакансий и резюме (любые URL).
- Динамическая диагностика – подсказки при ошибках внутри интерфейса, отдельные статусы.
- API ошибок логируются в деталях, быстро отображаются пользователю.
- Поддержка мобильных, быстрые паузы, новые лимиты и очищение истории.
- Продвинутая модульная структра кода (всё отделено: UI, лог, storage, responses, utils).

### v1.0

- Прототип: ручная отправка, базовый лог, простой интерфейс.

---

## 🆘 FAQ и поддержка

Если что-то не работает:
1. Проверьте корректность `RESUME_HASH` (без него скрипт не запустится!).
2. Посмотрите — нет ли ограничения от HH.ru (при слишком большом количестве откликов включается лимит).
3. Нажмите **Диагностику** — какие-то ошибки/рекомендации будут сразу видны в интерфейсе.
4. Все сообщения о сбоях пишутся и в консоль браузера: F12 → Console, всё системное попадёт туда.

Если скрипт реально помог или возникла проблема — оставьте отзыв, это помогает развитию проекта! ⭐

---

**Удачного поиска работы и светлых интервью!**
